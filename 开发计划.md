中断仿真模块 Python 代码开发计划
1. 任务边界
本开发计划只面向中断仿真模块，不包含级联失效模块，不展开重运营层建模，不直接求解库存、订单、排程、运输调度等业务细节。
当前系统研究对象限定为三部分：
供应商网络：供应商、工厂、关键供货关系、区域归属、替代关系等。
BOM 树/装配结构：产品—组件—零件—原材料之间的父子依赖关系。
供应商网络与 BOM 的映射关系：谁供应什么零件、是否单源、是否存在替代件、切换约束是什么。
本模块只研究：
外部中断场景如何注入；
中断如何沿“供应商—零件—组件—产品”映射链条传递；
不同处置/恢复措施如何影响恢复过程；
如何输出中断影响与恢复效果指标。
---
2. 当前开发前提与处理原则
你提到已有一些参考数据结构，放在 `input_data` 目录下。由于当前会话环境中我未直接读取到该目录内容，因此本计划采用“数据适配层优先”的实现路线：
不预设你的字段名完全固定；
第一阶段先写数据探测与标准化脚本；
将 `input_data` 中已有表结构统一映射到标准内部对象；
在此基础上开发中断仿真引擎。
这意味着第一版代码应优先具备：
自动读取 `csv/xlsx/json`；
字段别名映射；
缺失字段检测；
数据质量校验；
标准化导出。
这样后续即使你的 `input_data` 发生字段调整，也不用大改核心仿真逻辑。
---
3. 建议采用的总体技术路线
建议使用 Python + pandas + networkx + numpy 完成第一版研究型原型。
原因如下：
`pandas` 适合做多表读取、清洗、合并与映射；
`networkx` 适合构建供应商关系图、替代关系图、BOM 有向图；
`numpy` 适合做状态向量和批量实验；
第一版先追求“研究可复现、实验可批量运行、结构清晰”，不急于上数据库或前端。
后续如果要扩展成系统原型，再考虑：
`FastAPI`：提供场景配置与实验触发接口；
`plotly/pyecharts`：做恢复曲线和中断影响可视化；
`Neo4j`：当图规模更大、查询更复杂时再引入。
---
4. 中断仿真模块的核心设计思想
中断仿真模块的逻辑建议固定为五步：
加载基础数据：读取供应商网络、BOM 结构、映射关系。
注入中断场景：指定某个供应商、某类零件、某个区域或某组关系发生中断。
向 BOM 层传播影响：判断零件是否可得、组件是否可装、产品是否可完成。
执行处置/恢复策略：例如切换替代供应商、启用替代件、优先修复关键节点。
输出时序结果：恢复曲线、受影响产品数、关键件缺失情况、恢复时间等。
这里的关键不是做业务流程仿真，而是做结构化中断传播 + 轻量恢复机制。
---
5. 标准化数据模型设计
建议先统一成以下几类标准表。
5.1 供应商节点表 `suppliers`
建议字段：
`supplier_id`
`supplier_name`
`supplier_type`
`region`
`critical_level`
`status`（默认 available）
`recovery_time`
`capacity_factor`（默认 1.0）
`is_backup_candidate`
5.2 供应关系表 `supplier_edges`
若已有供应商之间的上下游依赖，可单独保留：
`source_supplier_id`
`target_supplier_id`
`relation_type`
`weight`
`dependency_level`
说明：这张表在中断模块中不是必须核心，但有助于定义区域性/簇性场景。
5.3 BOM 节点表 `bom_nodes`
建议字段：
`item_id`
`item_name`
`item_type`（product / assembly / part / material）
`critical_level`
`is_key_item`
`is_common_item`
5.4 BOM 边表 `bom_edges`
建议字段：
`parent_item_id`
`child_item_id`
`quantity`
`dependency_type`（AND / OR）
`is_mandatory`
说明：
`AND` 表示子件缺一个就不能装配；
`OR` 表示存在替代逻辑，可由多个子件之一满足。
5.5 供应商—零件映射表 `supplier_part_map`
这是核心表。
建议字段：
`supplier_id`
`item_id`
`share`
`is_primary`
`is_backup`
`switch_time`
`qualification_required`
`qualification_time`
`region`
5.6 替代件表 `part_alternatives`
建议字段：
`item_id`
`alt_item_id`
`alt_priority`
`switch_cost`
`switch_time`
`compatibility_score`
5.7 场景配置表 `scenarios`
建议字段：
`scenario_id`
`scenario_type`
`target_type`
`target_id`
`start_time`
`duration`
`severity`
`description`
5.8 恢复策略表 `policies`
建议字段：
`policy_id`
`policy_type`
`trigger_condition`
`priority_rule`
`cost`
`enabled`
---
6. 第一优先级：数据适配层开发
由于你的 `input_data` 目录目前更像“已有参考结构”而不是已经完全标准化的数据仓，第一优先级不是先写仿真引擎，而是先写数据适配层。
6.1 数据适配层职责
自动扫描 `input_data/` 下文件；
判断文件类型（csv、xlsx、json）；
对候选表做角色识别；
将原始字段映射为标准字段；
输出标准化 DataFrame；
生成数据质量报告。
6.2 角色识别建议
可通过字段关键词识别：
含 `supplier`, `vendor`, `供应商` 的表，优先识别为供应商表；
含 `bom`, `parent`, `child`, `组件`, `零件` 的表，优先识别为 BOM 表；
含 `map`, `part`, `item`, `source`, `share` 的表，优先识别为映射表；
含 `scenario`, `duration`, `severity` 的表，优先识别为场景表。
6.3 字段别名映射建议
例如：
`supplier_id` 可接受：`supplier_id`, `vendor_id`, `供应商编号`
`item_id` 可接受：`item_id`, `part_id`, `component_id`, `零件编号`
`parent_item_id` 可接受：`parent`, `parent_id`, `上级件号`
`child_item_id` 可接受：`child`, `child_id`, `下级件号`
建议把字段别名单独写入配置文件，例如：
```json
{
  "supplier_id": ["supplier_id", "vendor_id", "供应商编号"],
  "item_id": ["item_id", "part_id", "零件编号"],
  "parent_item_id": ["parent_item_id", "parent_id", "父件编号"],
  "child_item_id": ["child_item_id", "child_id", "子件编号"]
}
```
6.4 数据质量校验项
必须至少检查：
主键是否唯一；
BOM 是否有环；
映射表中的 `supplier_id` 是否存在于供应商表；
映射表中的 `item_id` 是否存在于 BOM 节点表；
单源件是否被正确标记；
替代件是否存在自循环；
场景表中的目标对象是否存在。
---
7. 中断仿真引擎的代码架构建议
建议项目目录按如下方式组织：
```text
supply_disruption_sim/
├── input_data/
├── config/
│   ├── field_alias.json
│   ├── default_params.yaml
│   └── scenario_templates.yaml
├── data_adapter/
│   ├── scanner.py
│   ├── schema_detector.py
│   ├── mapper.py
│   ├── validator.py
│   └── standardizer.py
├── model/
│   ├── supplier_graph.py
│   ├── bom_graph.py
│   ├── mapping_graph.py
│   └── state_model.py
├── disruption/
│   ├── scenario_loader.py
│   ├── scenario_injector.py
│   ├── propagation_engine.py
│   ├── recovery_engine.py
│   └── metrics.py
├── policy/
│   ├── supplier_switch.py
│   ├── part_substitution.py
│   ├── priority_repair.py
│   └── policy_manager.py
├── experiment/
│   ├── runner.py
│   ├── batch_runner.py
│   ├── monte_carlo.py
│   └── report_generator.py
├── viz/
│   ├── network_plot.py
│   ├── bom_plot.py
│   ├── timeline_plot.py
│   └── dashboard_data.py
├── tests/
└── main.py
```
---
8. 各子模块开发内容
8.1 `data_adapter/`：数据适配层
`scanner.py`
职责：
扫描 `input_data/` 中全部文件；
建立文件清单；
记录文件类型、工作表名、行列数。
`schema_detector.py`
职责：
根据字段名和内容特征推测表角色；
输出候选角色概率或得分。
`mapper.py`
职责：
将原始字段映射到标准字段；
支持手工配置覆盖自动判断。
`validator.py`
职责：
负责主键、外键、BOM DAG、映射一致性检查；
输出错误和警告列表。
`standardizer.py`
职责：
输出标准化 DataFrame；
存入内存对象或中间缓存目录。
---
8.2 `model/`：基础对象层
`supplier_graph.py`
职责：
将供应商节点表、供应关系表构建成有向图或属性图；
提供区域筛选、关键节点识别、相邻节点查询。
`bom_graph.py`
职责：
构建 BOM DAG；
提供从零件到组件到产品的向上追踪；
提供父件、子件、全祖先、全后代查询。
`mapping_graph.py`
职责：
连接供应商与零件；
实现“某供应商影响哪些件、哪些组件、哪些产品”的追踪函数。
`state_model.py`
职责：
统一维护仿真时刻的状态；
例如供应商状态、零件可得性、组件可装配性、产品可用性。
建议状态最小集合：
供应商：`available / disrupted / recovering`
零件：`available / degraded / unavailable`
组件：`assemblable / blocked`
产品：`active / affected / failed`
---
8.3 `disruption/`：中断仿真主引擎
`scenario_loader.py`
职责：
从配置文件或表格读取场景；
支持场景模板和参数覆盖。
`scenario_injector.py`
职责：
将中断事件注入到系统状态中；
支持以下中断类型：
单供应商中断
多供应商同步中断
区域性中断
零件级短缺
映射边中断（断供关系失效）
渐进式中断（降容→停供→恢复）
`propagation_engine.py`
职责：
根据映射关系推导零件可得性；
根据 BOM 逻辑向上推导组件与产品受影响状态；
支持替代逻辑和多源逻辑。
建议传播逻辑按以下顺序：
更新受影响供应商状态；
重新计算每个零件的可供应集合；
根据是否有可用来源判定零件状态；
自底向上遍历 BOM，判定组件可装配性；
自底向上更新产品状态。
`recovery_engine.py`
职责：
根据恢复时间推进状态变化；
调用策略模块执行恢复；
记录时间序列结果。
`metrics.py`
职责：
输出以下指标：
受影响供应商数
受影响零件数
受影响组件数
受影响产品数
最大功能跌幅
恢复时间 TTR
平均功能水平
关键产品维持率
策略成本
策略收益
---
8.4 `policy/`：恢复策略模块
`supplier_switch.py`
策略：备用供应商切换
逻辑：
若某零件主供中断；
检查是否存在备份供应商；
若存在，则考虑 `switch_time` 后恢复可得性；
若资格认证必需，则增加 `qualification_time`。
`part_substitution.py`
策略：替代件切换
逻辑：
若某关键件不可得；
检查替代件集合；
选取优先级最高且当前可得的替代件；
更新 BOM 依赖判定。
`priority_repair.py`
策略：优先修复
逻辑：
不按随机顺序恢复；
优先恢复影响产品最多、关键等级最高或共用性最高的节点/件。
`policy_manager.py`
职责：
统一管理策略启停；
定义多策略组合顺序；
计算策略总成本。
---
9. 核心算法实现建议
由于本模块只做中断仿真，因此核心不是复杂优化，而是可解释的规则引擎 + 时序推进。
9.1 时间推进方式
建议第一版采用离散时间步：
`t = 0, 1, 2, ... , T`
每个时间步执行：
注入新事件
更新恢复状态
执行策略
重新传播影响
记录指标
优势：
实现简单；
适合批量实验；
易于和场景表联动。
9.2 零件可得性判定函数
建议封装为：
```python
def evaluate_item_supply(item_id, time_t, suppliers_for_item, disrupted_suppliers, policy_state) -> str:
    ...
```
返回：
`available`
`degraded`
`unavailable`
9.3 组件装配判定函数
建议封装为：
```python
def evaluate_assembly_status(parent_item_id, child_statuses, dependency_rules) -> str:
    ...
```
判定逻辑：
`AND`：任一必需子件不可得，则 blocked
`OR`：至少一个子件可得，则 assemblable
9.4 产品影响回溯函数
建议封装为：
```python
def trace_impacted_products(item_id, bom_graph) -> list[str]:
    ...
```
用于回答：
某中断会影响哪些产品；
哪些产品最脆弱；
哪些零件是高影响共用件。
---
10. 推荐先实现的中断场景
第一版不要一开始就做太多复杂场景，建议按以下顺序实现：
场景1：单供应商停供
输入：
`supplier_id`
`start_time`
`duration`
输出：
受影响零件
受影响组件
受影响产品
恢复曲线
场景2：区域性中断
输入：
`region`
`start_time`
`duration`
`severity`
输出：
区域内停供节点数
关键产品恢复时间
场景3：关键件短缺
输入：
`item_id`
`start_time`
`duration`
输出：
影响产品覆盖率
替代件策略效果
场景4：多点同时中断
输入：
供应商列表 / 区域 + 类别
输出：
系统最大功能跌幅
策略组合优劣比较
---
11. 参数敏感度分析开发计划
在中断仿真跑通之后，必须加上参数敏感度分析模块，否则系统很难支撑论文输出。
建议重点分析以下参数：
单源比例
备份供应商覆盖率
替代件可用率
切换时间
资格认证时间
恢复时间分布
区域中断范围
BOM 深度
共用件比例
关键件比例
开发建议
第一版先做单因素敏感度分析；
第二版再做 Monte Carlo；
第三版可补 Morris/Sobol。
建议输出：
参数—指标折线图
敏感度排序表
不同场景下的稳健性比较
---
12. 推荐的开发阶段与优先级
阶段1：数据适配与标准化
目标：把 `input_data` 变成统一内部数据结构。
实现顺序：
文件扫描
字段识别
表角色识别
字段映射
数据校验
标准化输出
交付物：
标准表 DataFrame
数据质量报告
字段映射配置文件
阶段2：基础图模型
目标：构建供应商图、BOM 图、映射图。
实现顺序：
构建供应商图
构建 BOM DAG
建立供应商—零件索引
实现产品影响追踪函数
交付物：
图对象
基础查询接口
阶段3：中断注入与传播
目标：跑通单场景仿真。
实现顺序：
单供应商中断
零件级中断
自底向上 BOM 传播
时间推进与结果记录
交付物：
场景注入器
传播引擎
时间序列结果
阶段4：恢复策略
目标：支持最基本的处置模拟。
实现顺序：
备用供应商切换
替代件切换
优先修复
策略组合比较
交付物：
策略管理器
策略成本/收益比较结果
阶段5：实验批处理与敏感度分析
目标：支撑论文实验。
实现顺序：
批量运行器
参数扫描
Monte Carlo
结果汇总与导出
交付物：
批处理脚本
实验报告表格
结果图表
阶段6：可视化封装
目标：形成可展示原型。
实现顺序：
网络结构图
BOM 影响路径图
恢复曲线
场景对比图
交付物：
图表输出
演示用脚本
---
13. 建议的主程序调用流程
建议 `main.py` 的主流程固定如下：
```python
from data_adapter.standardizer import load_standard_data
from model.supplier_graph import build_supplier_graph
from model.bom_graph import build_bom_graph
from model.mapping_graph import build_mapping_index
from disruption.scenario_loader import load_scenario
from disruption.scenario_injector import inject_scenario
from disruption.propagation_engine import run_propagation
from disruption.recovery_engine import run_recovery
from disruption.metrics import summarize_results


def main():
    data_bundle = load_standard_data("input_data/")
    supplier_graph = build_supplier_graph(data_bundle)
    bom_graph = build_bom_graph(data_bundle)
    mapping_index = build_mapping_index(data_bundle)

    scenario = load_scenario("default_random_distributed_node_disruption")
    sim_state = inject_scenario(data_bundle, scenario)
    history = run_recovery(sim_state, supplier_graph, bom_graph, mapping_index)
    result = summarize_results(history)
    print(result)


if __name__ == "__main__":
    main()
```
---
14. 测试计划
建议把测试分成四层。
14.1 数据层测试
是否能识别不同表；
字段映射是否正确；
BOM 是否无环；
映射是否完整。
14.2 逻辑层测试
单个供应商中断后，是否正确定位受影响零件；
单个零件失效后，是否正确影响上层组件；
OR 替代逻辑是否生效。
14.3 场景层测试
区域性中断是否正确筛中目标节点；
多点中断是否能同时注入；
渐进式中断是否按时间推进。
14.4 策略层测试
备用供应商切换是否改变恢复曲线；
替代件策略是否缩短恢复时间；
优先修复是否改善关键产品维持率。
---
15. 第一版必须完成的最小可用原型（MVP）
如果只做第一版、追求最快形成研究型样机，建议只实现下面这些能力：
读取 `input_data` 中的供应商表、BOM 表、映射表；
自动标准化字段；
构建供应商图 + BOM DAG + 映射索引；
支持单供应商中断场景；
自底向上推导受影响产品；
支持一个恢复策略：备用供应商切换；
输出恢复时间、受影响产品数、功能跌幅；
导出结果到 `csv` 和图片。
只要这套 MVP 跑通，你就已经具备：
系统原型底座；
论文实验底座；
后续扩展级联失效模块的接口基础。
---
16. 对你当前最合适的开发建议
结合你现在的研究边界，我建议实际编码时遵循下面这个顺序：
第一步
先不要急着写复杂仿真，优先完成：
`scanner.py`
`schema_detector.py`
`mapper.py`
`validator.py`
理由：你的数据结构尚未完全锁定，适配层决定后续维护成本。
第二步
完成三个基础对象：
`SupplierGraph`
`BOMGraph`
`MappingIndex`
理由：只要这三个对象建好，中断仿真逻辑就能稳定复用。
第三步
实现最简单场景：
单供应商停供
备用供应商切换
产品影响回溯
理由：这是最容易验证正确性的闭环。
第四步
再逐步增加：
区域性中断
多点中断
替代件策略
批量实验与敏感度分析
---
17. 结论
本项目当前最合理的 Python 开发路线，不是直接做“大而全的供应链数字孪生系统”，而是先做一个围绕供应商网络—BOM 树—映射关系的中断仿真研究型原型。
其核心开发原则应当是：
先适配数据，再构建图模型；
先跑通单场景，再扩展多场景；
先做可解释规则，再做大规模实验；
先形成研究型 MVP，再扩展系统化可视化。
如果按本计划推进，第一版代码最有价值的成果将不是“界面”，而是：
一套稳定的数据适配与标准化机制；
一个可复用的中断传播与恢复引擎；
一套能支撑论文实验的批量场景分析框架。
